<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Beta-only components and their permissions (Bluetooth trigger, radar alert service,
         FCM push, Android Auto) live in src/dev/AndroidManifest.xml — they are merged into the
         dev flavor only, so the prod APK neither declares nor requests any of them. -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:name=".CompanionApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Companion">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Companion"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
              Password-reset links from Supabase come back here. A private URI scheme
              (per flavor, so the two APKs don't fight over it) rather than an App Link:
              verified App Links would need a domain we own, and this needs none.

              Not a security boundary — any app can claim a scheme. The link carries a
              single-use PKCE code that is exchanged over TLS for a session, so an
              intercepted redirect yields nothing reusable.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="auth-callback"
                    android:scheme="${authRedirectScheme}" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Started/stopped by the record button on MapScreen while "This phone" is the
             selected device (see LocalTrackingService); always started with the app in the
             foreground, so no ACCESS_BACKGROUND_LOCATION requirement. -->
        <service
            android:name=".car.LocalTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
